apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
# Base resources for Gateway class configuration
resources:
  - ../base
  - gatewayparameters.yaml
# Overlays specific to kgateway
patchesStrategicMerge:
  - gateway.yaml
  - httproute.yaml
