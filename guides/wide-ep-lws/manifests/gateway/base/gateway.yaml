apiVersion: gateway.networking.k8s.io/v1
kind: Gateway
metadata:
  name: infra-wide-ep-inference-gateway
spec:
  listeners:
    - port: 80
      protocol: HTTP
      name: default
      allowedRoutes:
        namespaces:
          from: All