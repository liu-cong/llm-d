apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
# Base resources for Gateway class configuration
resources:
  - ../base
  - desinationrule.yaml

# Overlays specific to Istio
patchesStrategicMerge:
  - gateway.yaml
  - httproute.yaml
