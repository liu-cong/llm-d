# precise-prefix-aware (Kustomize v2)

This directory contains the new modular Kustomize-based installation for the `precise-prefix-aware` guide.

## Installation

```bash
kubectl apply -k .
```

## Migration Status

This v2 guide is being developed alongside the existing Helm-based guide. Once validated, it will replace the Helm charts.
